# Eureka client configuration
eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

# MongoDB database configuration
spring:
  data:
    mongodb:
      uri: mongodb://mitocode:mitocode@mongo-service:27017/mitocode?authSource=admin
